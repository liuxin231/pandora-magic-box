import Developing from "@/components/Developing";
import { Typography } from "@mui/material";
import React from "react";

const KeyForm = () => {
  return (
    <Developing />
  );
};

export default React.memo(KeyForm);
